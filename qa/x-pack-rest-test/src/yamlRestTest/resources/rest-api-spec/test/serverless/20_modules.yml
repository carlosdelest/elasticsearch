"Analysis modules bundled":
    - skip:
        reason: "contains is a newly added assertion"
        features: contains
    - do:
        cluster.state: {}

    # Get master node id
    - set: { master_node: master }

    - do:
        nodes.info: {}

    - contains: { nodes.$master.modules: { name: analysis-icu } }
    - contains: { nodes.$master.modules: { name: analysis-nori } }
    - contains: { nodes.$master.modules: { name: analysis-smartcn } }
    - contains: { nodes.$master.modules: { name: analysis-ukrainian } }
    - contains: { nodes.$master.modules: { name: analysis-kuromoji } }
    - contains: { nodes.$master.modules: { name: analysis-phonetic } }
    - contains: { nodes.$master.modules: { name: analysis-stempel } }


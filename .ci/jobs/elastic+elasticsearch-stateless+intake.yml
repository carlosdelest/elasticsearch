---
- job:
    name: elastic+elasticsearch-stateless+intake
    display-name: "elastic / elasticsearch-stateless # intake"
    description: "Testing of Elasticsearch Stateless on every push.\n"
    workspace: /dev/shm/elastic+elasticsearch-stateless+intake
    project-type: multijob
    node: master
    vault: []
    triggers:
      - github
    builders:
      - multijob:
          name: Check
          projects:
            - name: elastic+elasticsearch-stateless+intake+multijjob+check
              kill-phase-on: NEVER
              current-parameters: true
              git-revision: true
      - multijob:
          name: Publish Artifacts
          projects:
            - name: elastic+elasticsearch-stateless+intake+multijob+publish-artifacts
              kill-phase-on: NEVER
              current-parameters: true
              git-revision: true

# In elasticsearch production code, this service is loaded from the modules path.
# Tests and the CLI tools use the classpath, so we need to define the service provider here.

co.elastic.elasticsearch.serverless.buildinfo.ServerlessBuildExtension
